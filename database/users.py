from .connection import get_connection

async def user_exists(user_id):
    # Placeholder: Always say user does not exist
    return False

async def create_user(user_id, username, rank, user_class, hp, attack, magic_attack, defense, speed, mana):
    # Placeholder: Do nothing for now
    pass
